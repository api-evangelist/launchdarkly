naftiko: 1.0.0-alpha2
info:
  label: LaunchDarkly REST API — Projects
  description: 'LaunchDarkly REST API — Projects. 5 operations. Lead operation: List projects. Self-contained Naftiko capability
    covering one Launchdarkly business surface.'
  tags:
  - Launchdarkly
  - Projects
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LAUNCHDARKLY_API_KEY: LAUNCHDARKLY_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-projects
    baseUri: https://app.launchdarkly.com/api/v2
    description: LaunchDarkly REST API — Projects business capability. Self-contained, no shared references.
    resources:
    - name: projects
      path: /projects
      operations:
      - name: listprojects
        method: GET
        description: List projects
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
          description: Maximum number of projects to return.
        - name: offset
          in: query
          type: integer
          description: Number of projects to skip for pagination.
        - name: filter
          in: query
          type: string
          description: A filter expression to apply to the project list.
      - name: createproject
        method: POST
        description: Create a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: projects-projectKey
      path: /projects/{projectKey}
      operations:
      - name: getproject
        method: GET
        description: Get a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patchproject
        method: PATCH
        description: Update a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteproject
        method: DELETE
        description: Delete a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.LAUNCHDARKLY_API_KEY}}'
  exposes:
  - type: rest
    namespace: rest-projects-rest
    port: 8080
    description: REST adapter for LaunchDarkly REST API — Projects. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/projects
      name: projects
      description: REST surface for projects.
      operations:
      - method: GET
        name: listprojects
        description: List projects
        call: rest-projects.listprojects
        with:
          limit: rest.limit
          offset: rest.offset
          filter: rest.filter
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createproject
        description: Create a project
        call: rest-projects.createproject
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/projects/{projectkey}
      name: projects-projectkey
      description: REST surface for projects-projectKey.
      operations:
      - method: GET
        name: getproject
        description: Get a project
        call: rest-projects.getproject
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchproject
        description: Update a project
        call: rest-projects.patchproject
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteproject
        description: Delete a project
        call: rest-projects.deleteproject
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-projects-mcp
    port: 9090
    transport: http
    description: MCP adapter for LaunchDarkly REST API — Projects. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: list-projects
      description: List projects
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-projects.listprojects
      with:
        limit: tools.limit
        offset: tools.offset
        filter: tools.filter
      outputParameters:
      - type: object
        mapping: $.
    - name: create-project
      description: Create a project
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rest-projects.createproject
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-project
      description: Get a project
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-projects.getproject
      outputParameters:
      - type: object
        mapping: $.
    - name: update-project
      description: Update a project
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: rest-projects.patchproject
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-project
      description: Delete a project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: rest-projects.deleteproject
      outputParameters:
      - type: object
        mapping: $.
